apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookfast-booking.fullname" . }}
  labels:
    {{- include "bookfast-booking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bookfast-booking.selectorLabels" . | nindent 4 }}
