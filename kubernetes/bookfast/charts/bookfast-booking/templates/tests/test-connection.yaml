apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bookfast-booking.fullname" . }}-test-connection"
  labels:
    {{- include "bookfast-booking.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bookfast-booking.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
